Mise à jour : Le hops=2 est fonctionnel

- Pour la map : http://www.openstreetmap.org, https://planet.osm.org

- Pour résumer ce que j'ai fait : d'abord on crée le fichier schema qui sert à crée la table
* Ensuite on va dans postgreSQL pour crée notre table (qui sera donc vide)
* Ensuite on modifie le Myparse.csv (ce fichier sert à mettre des INSERT INTO ... values (valeur1,valeur2 ...) autant de fois qu'il y'a de ligne dans le fichier .csv
* (Dans Myparse)Ne pas oublier de modifier le nom du fichier en haut, et le nom de la table dans le INSERT INTO ...

- dans le for i in range(0,5), ça prend toute les valeurs de 0 à 4

- Le type numeric[] dans les schemas sert à définir des tableaux de int
* Pour accéder au premier élément de (1:2) faire counts[1] (l'indice 0 ne sert à rien ) (à vérifier !!!)
